# 🏭 智能制造：产线 OEE 分析与瓶颈识别系统极简 PRD

## 1. 项目定位与核心价值 (Why & Value)

* **业务痛点**：传统制造车间（如消费电子总装线）的传感器数据利用率低，工业工程师（IE）依靠人工掐秒表统计工时，效率低且存在严重的滞后性。
* **核心价值**：基于工业 4.0 真实产线传感器日志，利用 Python 进行实时数据清洗与 OEE 指标建模，实现**分钟级的效率监控、瓶颈自动诊断与工艺参数质量预警**。
---

## 2. 数据源定义 (Data Source)

* **选用数据集**：国际通用 UCI AI4I 2020 Predictive Maintenance Dataset（10,000 条真实数字化车间运行日志）。
* **核心字段与 IE 语义映射表**：

| 原始字段名 | 物理含义 | 映射后变量名（符合编程规范） | 工业工程（IE）业务语义转化逻辑 |
| --- | --- | --- | --- |
| `Type` | 产品型号 | `product_type` | H/M/L 三种生产线（高/中/低端产品线） |
| `Air temperature [K]` | 环境温度 | `air_temp` | 辅助监控工艺环境波动 |
| `Process temperature [K]` | 工艺温度 | `process_temp` | 影响 **Quality（良率）** 的核心焊接/注塑工艺特征 |
| `Rotational speed [rpm]` | 设备转速 | `rotational_speed` | 对应 **Performance**：转速低意味着实际节拍变慢 |
| `Torque [Nm]` | 设备扭矩 | `torque` | 对应 **Performance**：扭矩异常意味着工装过载或滑丝 |
| `Tool wear [min]` | 刀具磨损时间 | `tool_wear` | 对应 **Availability**：磨损到期必须停机换型（SMED） |
| `Machine failure` | 是否故障 | `is_failure` | 0 代表正常运行，1 代表停机损失（时间利用率核心指标） |

---

## 3. 业务逻辑与核心算法 (IE Methods)

### 维度一：OEE 三维指标建模

设定单台设备单日总计划生产时间为 **1440 分钟（24小时）**。

1. **Availability（时间利用率）** = $\frac{\text{计划时间} - \text{停机损失时间}}{\text{计划时间}}$
* *计算规则*：`is_failure == 1` 每次计为 30 分钟故障停机；`tool_wear` 累计达到 200 分钟时计为一次 45 分钟的“换模/换刀停机”。


2. **Performance（性能效率）** = $\frac{\text{实际转速}}{\text{标准理论转速}}$
* *计算规则*：该产线标准理论转速定为 1500 rpm。若实际转速 / 1500 > 1.0，则截断为 1.0（防止效率超标）。


3. **Quality（合格率）** = $\frac{\text{总产量} - \text{缺陷数}}{\text{总产量}}$
* *计算规则*：数据集中 `TWF/HDF/PWF/OSF` 任意一项为 1，即判定该产品为不合格品（缺陷件）。



### 维度二：瓶颈诊断与智能决策

* **低效工序自动标红**：系统自动计算并在看板中高亮标出 OEE 最低的产线。
* **损失根因分析**：统计五大失效模式（散热、功率、过载、磨损等）的频次，绘制排列图（Pareto 图）。
* **IE 知识库联动**：
* 若 HDF（散热失效）占比最高 $\rightarrow$ 提示：“建议引入智能冷却循环工装，优化温控工艺。”
* 若 TWF（刀具磨损）占比最高 $\rightarrow$ 提示：“建议实施 SMED 快速换型策略，优化换刀节拍。”



---

## 4. 产品功能看板定义 (User Interface Scope)

系统采用 **Streamlit** 进行全栈开发，严禁在 48 小时内增加以下三个页面之外的任何冗余功能：

```
[ 侧边栏导航 Sidebar Navigation ]
 ├── 页面 1：生产运营总览 (Dashboard)  ──> 展示全线 OEE、多产线柱状图对比
 ├── 页面 2：瓶颈工序诊断 (Diagnostic) ──> 损失根因 Pareto 图 + 自动输出 IE 改善意见
 └── 页面 3：工艺参数模拟 (Simulator)  ──> 滑动条调节温度/转速/扭矩，实时进行质量风险预警

```

### 页面详细交互规范：

* **Page 1：生产运营总览**
* 顶部通过 `st.metric` 大字显示全线平均 OEE、Availability、Performance、Quality。
* 中间使用条形图展示 H、M、L 三条产线的指标对比，一眼看出哪条线在拖后腿。

* **Page 2：瓶颈工序诊断**
* 渲染损失原因的饼图或柱状图。
* 动态生成文本框，展示系统针对最严重问题给出的精益改善意见。

* **Page 3：实时工艺参数模拟器（数字化 NPI 场景）**
* 提供 3 个 `st.slider` 滑动条：环境温度（290k-310k）、转速（1000-2800rpm）、扭矩（10-80Nm）。
* 页面下方实时显示当前参数组合下的**质量缺陷风险等级**（绿/黄/红分级预警）。

---

## 5. 交付物标准 (Deliverables)

1. 包含 `main.py` 和 `app.py` 且代码结构整洁的本地运行系统。
2. 规范的 Git Commit 历史（禁止只有一次提交就收尾）。
3. 包含核心看板运行截图、架构说明和启动命令的工业级 `README.md` 文档。